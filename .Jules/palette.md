## 2024-05-04 - Explicit Optional Fields
**Learning:** Explicitly marking optional input fields (like Description, Source) with `(opt)` in CLI forms significantly reduces cognitive load and user hesitation when asking for multiple fields.
**Action:** Always append dim `(opt)` tags to optional prompt inputs in CLI and interactive CLI setups to improve clarity.
