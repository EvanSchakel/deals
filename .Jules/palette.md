## 2024-05-18 - Mark Optional CLI Fields
**Learning:** CLI applications with multiple inputs should clearly indicate which fields are optional, preventing user hesitation and making the form easier to scan.
**Action:** Add `(opt)` with dim styling to optional fields in interactive prompts.
