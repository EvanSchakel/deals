## 2024-05-24 - Explicitly Marking Optional CLI Fields
**Learning:** Explicitly marking optional CLI input fields (e.g., with `(opt)` in dim text) reduces cognitive load and prevents user hesitation, making the CLI interaction smoother and more accessible.
**Action:** Always ensure optional fields in command-line interfaces or forms are clearly indicated to avoid user confusion.
